#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> arr = {5, 3, 4, 1, 2};

    /* first sort the array using quick sort */
    /* then last element of sorted array will be the largest element */
    return 0;
}

/* time complexity - O(n * log n), T.C of quick sort */