#include <iostream>
using namespace std;
int main() {
    float x=2.3, y=3;
    // We can store integers values in float datatypes but on storing decimal values in int datatype it will do GIF of it
    cout << "Values of x and y are : " << x << " and " << y << endl;
    return 0;
}